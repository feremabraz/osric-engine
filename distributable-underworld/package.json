{
  "name": "@osric/distributable-underworld",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "pnpm -w -F @osric/viewer-underworld build && electron .",
    "build": "tsc -p tsconfig.json",
    "pack": "electron-builder --dir",
    "dist": "pnpm -w -F @osric/viewer-underworld build && pnpm -F @osric/distributable-underworld build && rimraf dist/app && mkdir -p dist/app && cp -r ../viewer-underworld/dist/* dist/app/ && electron-builder"
  },
  "dependencies": {},
  "devDependencies": {
    "electron": "^31.4.0",
    "electron-builder": "^24.13.3",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.3"
  }
}
