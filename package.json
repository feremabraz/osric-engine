{
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^20",
    "husky": "^9.1.7",
    "typescript": "^5",
    "vitest": "^3.1.3"
  },
  "name": "osric-engine",
  "packageManager": "pnpm@10.11.0",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit && biome lint && vitest run",
    "format": "biome format --write .",
    "lint": "biome lint --write .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prepare": "husky"
  },
  "version": "0.4.0"
}
